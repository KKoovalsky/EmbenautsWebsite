---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body>
    <header>
      <div>
        <a href="/" class="brand">
          <img src="/images/logo.png" alt="Embenauts logo" class="brand-logo" />
          <span class="brand-name">Embenauts</span>
        </a>
      </div>
      <nav>
        <a href="/blog">Articles</a>
        <a href="/about">About</a>
        <a href="mailto:kacper@embenauts.dev">Contact</a>
      </nav>
    </header>

    <main>
      <slot />
    </main>
  </body>
</html>

<style>
.brand {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  color: var(--blue);
  text-decoration: none;
}

.brand:hover {
  text-decoration: none;
}

.brand-logo {
  height: 48px;
  width: 48px;
  object-fit: contain;
}

.brand-name {
  color: var(--blue);
  font-weight: 600;
}
</style>
